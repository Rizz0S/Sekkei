<!DOCTYPE html>
<html>
<head>

<%= csrf_meta_tags %>
<%= csp_meta_tag %>
<%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
<%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
<div>
<nav>

<div class="nav-left-edge-container">
    <div class="nav-bar-headers-background">
        <h1 class="page-header">∵ Sekkei ∴</h1>
    </div>
</div>


<h4 class="nav-bar-headers">
    <div class="nav-bar-headers-background">
        <%= link_to("Origamis", origamis_path)%> //
        <%= link_to("Posts", posts_path) %> //
        <%= link_to("Origami Search", origamis_search_path) %> 
    </div>
</h4>


<div class="nav-right-edge-container">
<h4 class="nav-links-right">
    <div class="nav-bar-headers-background">
        <% if @logged_in_user %>
            Welcome, <%= link_to(@logged_in_user.name, user_path(@logged_in_user.id)) %>!
             ┊ <%= link_to("🛒", my_basket_path) %>
             ┊ <%= link_to("Logout", logout_path, :method => 'delete') %>
        <% else %>
            <%= link_to("Login", login_path) %>
        <%end%>
    </div>
</h4>
</div>

</nav>
</div>
</head>
<body style="background-color:#ffe6e6">
    <%= yield %>
</body>
  
</html>
